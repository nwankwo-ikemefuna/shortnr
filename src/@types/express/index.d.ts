/**
 * Note: this is an ambient (global) module, top-level imports are not allowed!
 */

declare namespace Express {
    interface Request {
        //add objects that extend the Express.Request object here
    };
};